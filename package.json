{
  "name": "yaas",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "db:generate": "pnpm --filter @yaas/db generate",
    "db:migrate": "pnpm --filter @yaas/db migrate",
    "db:push": "pnpm --filter @yaas/db push",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
